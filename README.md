# 🚀 Crash Rocket — Telegram Mini App Game

## Описание

Онлайн-игра **Crash (Ракетка)** в стиле 1win, адаптированная под телефон и интегрированная с Telegram (Mini App).

Ракетка взлетает, множитель растёт — успей забрать до взрыва! При коэффициенте **3x и выше** — выигрывай NFT подарки!

## Механика

- Ставка → Ракетка взлетает → Множитель растёт (1.00x — 50.00x)
- Успей нажать "ЗАБРАТЬ" до взрыва
- При **3x+** — получаешь NFT подарок (тир зависит от множителя)
- Крупная ставка (10+ TON) — превью подарка через ~5 секунд
- Если не забрал до взрыва — проигрыш

## Депозит (4 способа)

| # | Способ | Описание |
|---|--------|----------|
| 1 | 🎁 Telegram Подарки (NFT) | Отправьте NFT подарок боту |
| 2 | 👛 Крипто кошелёк | TON, USDT, BTC, ETH, SOL |
| 3 | 🤖 CryptoBot | Инвойс через @CryptoBot |
| 4 | ⭐ Telegram Stars | Оплата звёздами Telegram |

## Вывод

**Только Telegram NFT подарки!** Деньги и крипта не выводятся.

Подарки зависят от достигнутого множителя:

| Множитель | Подарок |
|-----------|---------|
| 3x — 5x | 🎁 Bronze Gift |
| 5x — 10x | 🎄 Silver Gift |
| 10x — 20x | 🏆 Gold Gift |
| 20x — 35x | 👑 Platinum Gift |
| 35x — 50x | 💰 Diamond Gift |

## Установка

```bash
# 1. Установить зависимости
npm install

# 2. Настроить .env файл
cp .env.example .env
# Заполнить BOT_TOKEN и WEBAPP_URL

# 3. Запустить сервер
npm start

# 4. Запустить бота (в отдельном терминале)
npm run bot
```

## Настройка для продакшена

1. **Домен**: Поставьте сервер за nginx/reverse proxy с HTTPS
2. **BOT_TOKEN**: Получите у @BotFather
3. **WEBAPP_URL**: Укажите ваш домен с HTTPS (обязательно для Telegram Mini Apps)
4. **CryptoBot**: Получите API токен у @CryptoBot
5. **Mini App**: Настройте Web App URL в @BotFather → Bot Settings → Menu Button

## Структура проекта

```
bot/
├── server.js          # Express + Socket.IO сервер
├── bot.js             # Telegram бот
├── config.js          # Конфигурация
├── package.json
├── .env               # Секреты (не коммитить!)
├── database/
│   └── db.js          # SQLite база данных
├── game/
│   └── crash.js       # Игровой движок Crash
├── routes/
│   ├── api.js         # API маршруты (авторизация, профиль)
│   └── deposit.js     # Депозит и вывод
├── public/
│   ├── index.html     # Главная страница
│   ├── css/
│   │   └── style.css  # Стили (мобайл-first)
│   └── js/
│       └── app.js     # Клиентское приложение
└── data/
    └── game.db        # SQLite файл (создаётся автоматически)
```

## Технологии

- **Backend**: Node.js, Express, Socket.IO
- **Database**: SQLite (better-sqlite3)
- **Frontend**: Vanilla JS, Canvas API
- **Bot**: node-telegram-bot-api
- **Realtime**: WebSocket (Socket.IO)
